Infrastructure (placeholders)

This folder will contain Terraform, Kubernetes manifests, CI/CD pipeline templates and Docker images.

Next steps:
- Add Terraform for AWS: VPC, EKS, RDS(Postgres+PostGIS), ElastiCache (Redis), S3 buckets.
- Add Kubernetes manifests and Helm charts for deploying `api` and `admin` services.
- Add GitHub Actions workflows for CI/CD.
